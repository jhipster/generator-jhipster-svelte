{
	"printWidth": 80,
	"tabWidth": 4,
	"useTabs": true,
<%_
if (!skipClient) { _%>
	"semi": false,
	"singleQuote": true,
	"arrowParens": "avoid",
	"svelteSortOrder": "options-scripts-styles-markup",
	"svelteStrictMode": true,
	"svelteBracketNewLine": true,
	"svelteAllowShorthand": true,
<%_ } _%>
	"plugins": [<%_ if (!skipClient) { _%>"prettier-plugin-svelte"<%_ } if(!skipServer) { _%><%_ if (!skipClient) { _%>, <%_ } _%>"prettier-plugin-java"<%_ } _%>],
	"overrides": [
		{
			"files": "src/test/resources/**/*.html",
			"options": {
				"printWidth": 140
			}
		}<% if (!skipServer) { %>,
		{
			"files": "*.java",
			"options": {
				"useTabs": false,
				"printWidth": 140
			}
		}<% } %>
	]
}
