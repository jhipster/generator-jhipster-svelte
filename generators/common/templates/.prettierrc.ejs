{
	"printWidth": 100,
	"tabWidth": 4,
	"useTabs": true,
<%_
if (!skipClient) { _%>
	"semi": false,
	"singleQuote": true,
	"arrowParens": "avoid",
	"svelteSortOrder": "options-scripts-styles-markup",
	"svelteStrictMode": true,
<%_ } _%>
	"overrides": [
		{
			"files": "src/test/resources/**/*.html",
			"options": {
				"printWidth": 140
			}
		}<% if (!skipServer && javaPrettier) { %>,
		{
			"files": "*.java",
			"options": {
				"useTabs": false,
				"printWidth": 140
			}
		}<% } %>
	]
}
