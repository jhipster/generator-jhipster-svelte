{
	"printWidth": 100,
	"tabWidth": 4,
	"useTabs": true,
<%_
if (!skipClient) { _%>
	"semi": false,
	"singleQuote": true,
	"arrowParens": "avoid",
	"svelteSortOrder": "options-scripts-styles-markup",
	"svelteStrictMode": true,
<%_ } _%>
	"plugins": [
<%_ if (!skipServer && backendTypeJavaAny) { _%>
		"prettier-plugin-java",
<%_ }
	if (!skipClient) { _%>
_%>
		"prettier-plugin-organize-imports",
		"prettier-plugin-packagejson",
		"prettier-plugin-svelte"
<%_ } _%>
	],
	"overrides": [
		{
			"files": "src/test/resources/**/*.html",
			"options": {
				"printWidth": 140
			}
		}<% if (!skipServer && backendTypeJavaAny) { %>,
		{
			"files": "*.java",
			"options": {
				"useTabs": false,
				"printWidth": 140
			}
		}<% } %>
	]
}
